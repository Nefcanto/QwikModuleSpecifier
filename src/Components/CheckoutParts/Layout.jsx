import { component$ } from "@builder.io/qwik"
import { LayoutWrapper } from "CheckoutParts"

const Layout = component$((props) => {

    return <LayoutWrapper {...props} />

})

export default Layout
